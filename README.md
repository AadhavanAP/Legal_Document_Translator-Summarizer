# 🌍 Translation App

A Streamlit-based app that extracts text from images or PDFs using OCR and translates it to multiple languages using Hugging Face's `nllb-200` model. Supports multilingual input, automatic language detection, and robust retry logic for API reliability.

## Setup

```bash
pip install -r requirements.txt
python complete-translation-app.py
```
